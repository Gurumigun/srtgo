services:
  srtgo-bot:
    build: .
    container_name: srtgo-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
    environment:
      - SRTGO_DB_PATH=/app/data/srtgo.db

volumes:
  bot-data:
