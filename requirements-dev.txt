# 개발용 의존성 라이브러리
# 먼저 requirements.txt를 설치한 후 이 파일을 설치하세요:
# pip install -r requirements.txt
# pip install -r requirements-dev.txt

# 런타임 의존성
-r requirements.txt

# 빌드 도구
setuptools
setuptools-scm
build
wheel

# 테스트 및 품질 관리
pytest
pytest-cov
flake8
black
mypy
